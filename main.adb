with MicroBit.IOsForTasking;
with MicroBit;
with MicroBit.Console; use MicroBit.Console;
with Directions;

procedure Main with Priority => 0 is --Set interrupt priority P to 0, the lowest priority
begin
   loop
      null;
   end loop;
end main;
